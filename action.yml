inputs:
  name:
    description: 'What is a name?'
    required: true
  phrase:
    description: 'A phrase the name says'
    required: true

runs:
  using: 'script'
  runtime: 'python3'
  main: 'hello.py'